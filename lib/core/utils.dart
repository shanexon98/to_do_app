import 'package:flutter/material.dart';

class AppColors {
  // Colores principales
  static const Color primary = Colors.green;
  static const Color secondary = Colors.orange;
  static const Color accent = Color(0xFF64DD17);

  // Transparencias
  static const Color transparentBlack = Color(0x88000000);
  static const Color transparentWhite = Color(0x88FFFFFF);
}
